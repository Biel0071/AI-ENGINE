global:
  scrape_interval: 15s
  evaluation_interval: 15s
scrape_configs:
  - job_name: fenix-api
    authorization:
      type: Bearer
      credentials_file: /run/secrets/metrics_token
    static_configs:
      - targets: ["api:4400"]
