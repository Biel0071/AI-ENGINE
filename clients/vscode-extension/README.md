# FÊNIX OS - VS Code Extension SDK

Extensão para VS Code permitindo que o desenvolvedor crie missões, invoque Capabilities locais, injete contexto de código e faça testes unitários de Plugins diretamente da IDE.

A extensão se comunica via `http://localhost:4400` com o Daemon.
