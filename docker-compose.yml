version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  docling:
    image: ghcr.io/ibm/docling
    ports:
      - "8000:8000"

volumes:
  qdrant_data: